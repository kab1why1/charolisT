@model IEnumerable<charolis.Models.RegUser>

<style>
        /* General Page Styling */
        body {
            font-family: Arial, sans-serif;
            background: #ecf0f1;
            color: #2c3e50;
            margin: 0;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h2 {
            color: #34495e;
            margin-bottom: 20px;
        }

        /* Table Styling */
        table {
            width: 90%;
            max-width: 1000px;
            border-collapse: collapse;
            background: white;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            border-radius: 8px;
            overflow: hidden;
            text-align: left;
        }

        th, td {
            padding: 12px;
            border-bottom: 1px solid #ddd;
        }

        th {
            background: #2c3e50;
            color: #ecf0f1;
            text-align: left;
        }

        tr:nth-child(even) {
            background: #f8f9fa;
        }

        tr:hover {
            background: #f1f1f1;
            transition: background 0.3s;
        }

        a {
            text-decoration: none;
            color: #3498db;
            font-weight: bold;
            transition: color 0.3s;
        }

        a:hover {
            color: #1abc9c;
        }

        /* Buttons */
        .button {
            display: inline-block;
            padding: 12px 20px;
            margin: 15px;
            background: #1abc9c;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background 0.3s;
        }

        .button:hover {
            background: #16a085;
        }
    </style>

    <h2>Список зареєстрованих користувачів</h2>

    <table>
        <tr>
            <th>Id</th>
            <th>Ім'я</th>
            <th>Email</th>
            <th>Телефон</th>
            <th>Адреса</th>
            <th>Дії</th>
        </tr>
        @foreach (var reguser in Model)
        {
            <tr>
                <td>@reguser.Id</td>
                <td>@reguser.Name</td>
                <td>@reguser.Email</td>
                <td>@reguser.Phone</td>
                <td>@reguser.Address</td>
                <td>
                    <a href="@Url.Action("EditRegUser", new { id = reguser.Id })">Редагувати</a> |
                    <a href="@Url.Action("DeleteRegUser", new { id = reguser.Id })" style="color: red;">Видалити</a>
                </td>
            </tr>
        }
    </table>

    <a href="@Url.Action("CreateRegUser")" class="button">Додати користувача</a>
    <a href="@Url.Action("Main")" class="button">⬅ Назад</a>